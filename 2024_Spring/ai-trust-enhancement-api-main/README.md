# Notes

I used pycharm, idk if it will work in vscode. This api uses a project called sherlock which gets social media links for a username.
This needs to be installed somewhere on your computer.

I used my reddit api key, which should remain private, to access praw, which is a python wrapper for the reddit api.